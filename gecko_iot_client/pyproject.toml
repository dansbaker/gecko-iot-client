[project]
name = "gecko-iot-client-community"
version = "0.3.0"
description = "Community fork of the Gecko IoT client with fixes & extensions for the Home Assistant integration"
authors = [
    {name = "alex-reid-gecko", email = "areid@geckoal.com"}
]
maintainers = [
    {name = "Dan Baker", email = "dansbaker@users.noreply.github.com"}
]
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">=3.13"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Hardware",
    "Topic :: Internet :: WWW/HTTP :: WSGI :: Application"
]
keywords = ["gecko", "iot", "aws", "spa", "hot-tub"]
dependencies = [
    "awscrt>=0.19.0",
    "awsiotsdk>=1.28.1",
    "aiohttp>=3.13.0",
]

[project.urls]
Homepage = "https://github.com/dansbaker/gecko-iot-client"
Repository = "https://github.com/dansbaker/gecko-iot-client.git"
Issues = "https://github.com/dansbaker/gecko-iot-client/issues"
Upstream = "https://github.com/geckoal/gecko-iot-client"

[project.optional-dependencies]
docs = [
    "sphinx>=7.0.0",
    "sphinx-autodoc-typehints>=1.24.0",
    "sphinx-rtd-theme>=1.3.0",
    "myst-parser>=2.0.0",
    "sphinxcontrib-napoleon>=0.7",
    "sphinx-autobuild>=2024.4.16",
    "awscrt>=0.19.0",
    "awsiotsdk>=1.21.0"
]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "isort>=5.12.0",
    "flake8>=6.0.0",
    "auto-changelog>=0.6.0",
    "setuptools-scm>=8.0",
    "pre-commit>=3.5.0"
]

[project.scripts]
gecko-build-docs = "gecko_iot_client.docs:build_docs"
gecko-build-changelog = "gecko_iot_client.changelog:build_changelog"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["py.typed"]

[tool.isort]
profile = "black"
